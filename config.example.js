// Schema reference for forks. Real config is in config.js (committed). Client/Tenant
// IDs are public app identifiers, not secrets. Put actual secrets in config.local.js.
window.MEDHUB_CONFIG = {
  clientId: 'REPLACE_ME',
  tenantId: 'REPLACE_ME',
};
